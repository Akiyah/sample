Xzibit = Object clone do(
  dawg = method(what,
    "yo dawg" print.
    what print.
    "clone" print.
    self
  ).
  
  so_you_can = method(
    "so you can print" print.
    self
  ).

  while = method(
    "while you print" print.
    self
  ).
  
  in_ur = method(
    "in ur clone" print.
    self
  ).
).

yo = Xzibit clone.
yo dawg("I put a") clone in_ur clone so_you_can(print) while(you print).

x = list(1, 2, 3).
x at(0) print.

ruby("puts 'im in ur ruby nao, o damn dawg!'").
